{% extends 'base.html.twig' %}

{% block title %}Rights{% endblock %}

{% block body %}
    <h1>Rights</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ right.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ right.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rights_index') }}">back to list</a>

    <a href="{{ path('app_rights_edit', {'id': right.id}) }}">edit</a>

    {{ include('rights/_delete_form.html.twig') }}
{% endblock %}
